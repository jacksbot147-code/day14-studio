# Nightly polish — 2026-06-11

**Target:** `src/lib/skills/customer-ltv-calculator.ts` (hand-coded skill)
**Commit:** `65c08a5` (local only, on `redesign/apple-base44-2026-06-03`)
**Why this target:** of the five core hand-coded skills not yet polished, it was the only one with ZERO `auditLog`/`logSkillInvocation` calls and the thinnest test file (4 cases). Last night was uptime-monitor, so skills rotation continued.

## Gaps found vs SKILL.md spec + CLAUDE.md hard rules

1. **No growth hook** (CLAUDE.md rule 6 + spec's auto-attached growth hook): `run()` never called `logSkillInvocation`. Fixed — fires on every invocation, feeds growth-watcher/skill-coverage-auditor.
2. **No audit trail** (CLAUDE.md rule 5): report generation drives acquisition-spend decisions but left no audit entry. Fixed — `ltv_report_generated` entry with aggregates in details.
3. **Spec failure mode ignored**: "<30 days → show TBD rather than guess" — code projected full LTV for brand-new customers. Fixed: new `projection_tbd` flag, projected_remaining conservatively 0, report renders "TBD" with an aggregate footnote. (Additive field — `churn-risk-scorer`'s use of `total_projected` unaffected.)
4. **Spec rule 2 ("always show the assumption") unmet**: report showed segment but not the churn rate used. Fixed: per-row "Churn assumption" column + new "Assumptions" section listing the default rates and explicitly flagging that they're defaults, not measured from cancellation history (that remains a known gap — needs cancellation-history data).
5. **Spec input `customer_slug` ignored**: `/ltv {slug}` Telegram route (dispatch.ts:88) got the full book back. Fixed: single-customer mode returns `customer` + `customer_found` in the outcome result. Report stays full-book.
6. **Double computation**: `run()` called `computeAllLtv()` twice (once inside `writeLtvReport`, once directly) — wasteful and racy across a month boundary. Fixed: `writeLtvReport(precomputed?)`.
7. **No error handling in `run()`**: any fs throw escaped raw. Fixed: try/catch → `ok: false` outcome with message.
8. **Unguarded-index style**: `sorted[i]!` swapped for guard-and-break per quality bar.

## Tests: 4 → 10 cases

Added: paused customer projects 0; <30d customer is TBD/conservative-0; malformed `01-brand.json` doesn't throw; report contains assumptions + TBD rendering; `run()` outcome shape + single-customer mode + work-register and audit entries actually land (verified against swapped `$HOME`); unknown slug → `customer_found: false` but still ok.

## Bonus fix (build break)

Repo-wide `tsc --noEmit` was FAILING before tonight: `src/components/motion/OrreryLive.tsx(136)` — `planetPulse`'s `ease: "easeInOut"` widened to `string`, rejected by framer-motion's `Easing` type. One-token fix in `src/lib/motion.ts` (`as const`). `npm run build` would have failed on this; now `tsc --noEmit` exits 0 with zero errors.

## Verification

- `tsc --noEmit` (project tsconfig, strict + noUncheckedIndexedAccess): **clean, exit 0**.
- vitest cannot run in the sandbox (node_modules built for darwin-arm64; rollup native binary missing on Linux). Tests verified by typecheck + inspection: the swapped-`$HOME` pattern works because work-register.ts and audit-log-generator.ts both resolve paths from `homedir()` at module level and `vi.resetModules()` re-evaluates the whole import graph. **Jack: run `npm test` on the Mac to confirm the 6 new cases.**

## Decisions made (no user present)

- TBD customers' `projected_remaining` set to 0 (conservative) rather than excluded from aggregates — keeps `computeAllLtv()`'s return shape and churn-risk-scorer's consumption backward-compatible.
- Spec rule 3 ("never round") interpreted as whole-dollar precision being fine; $11.0k vs $11.2k stays distinguishable. Left `Math.round` as-is.
- Spec's per-tier/vertical churn-from-cancellation-history left unimplemented — needs data that doesn't exist yet; documented in the report's Assumptions section instead of guessing.
