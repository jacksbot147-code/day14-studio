# Nightly polish — 2026-06-04

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: WebFetch returned full rendered text first try, no retry, no timeout. Hard ms unavailable from the sandbox — Vercel Speed Insights is the source of truth.
- Pages checked: 8 total (4 mandatory + 4 spot-check)
  - 200 on `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools`
  - 200 on `/stack`, `/work-with-us`, `/builds`, `/newsletter`
  - "Day14" present in every response body
- Issues found: 4 (1 new, 3 carried over from 06-03 — none fixed yet)

## Customer sites
- `~/Documents/businesses/day14/customers/` still empty — no `05-launch.md` files, nothing with status `launched`. Skipped, same as last night.

## Issues to surface
- **NEW: homepage ships a dev note to visitors.** The "Watch the system run" Loom panel literally reads "Demo recording in progress. Paste the Loom share URL into `LOOM_EMBED_URL` in `page.tsx`." That's an internal TODO rendered on production, above the fold of the pivot story, three days before the Sunday waitlist deadline.
- **Carryover: pitch drift.** `/about`, `/compare`, `/builds`, the case studies still sell the 14-day agency ($2,500/$5,000/$10,000); homepage sells the OS (Solo/Portfolio/Founder). Second night running.
- **Carryover: nav anchor split.** Homepage nav uses `#pricing`; every other page links `/#sku` (and `/compare` + `/builds` still render the old pre-pivot header with "Verticals" / "How it works"). Those anchors likely no longer exist on the re-sectioned homepage — silent dead links.
- **Carryover: `/compare` footnote still says "as of May 2026."** It's June.

## Recommended actions for Jack tomorrow
1. Paste the Loom URL into `LOOM_EMBED_URL` (or hide the panel until the demo exists). Five minutes, highest-visibility fix on the list — the waitlist closes Sunday.
2. One grep-and-sed pass: `#sku` → `#pricing` sitewide, and swap the stale header on `/compare` + `/builds` for the current one.
3. Bump the `/compare` footnote to June 2026 while you're in there.
