# Nightly polish — 2026-06-09

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: <3s (WebFetch returned fast; exact ms not measurable from sandbox — Lighthouse unavailable, fell back to 200 + "Day14" text check, which passed)
- Pages checked: 8 total — /, /about, /compare, /case-studies/splash-jacks-pools, /stack, /work-with-us, /builds, /newsletter. All 200, all render expected content.
- Issues found: 2, list below

## Issues to surface
- **Stale homepage anchors after the OS pivot.** Interior pages (/stack, /compare, /builds, /newsletter, /work-with-us, case studies) still link to `/#sku`, `/#verticals`, `/#faq` — the new OS homepage uses `#pricing`, `#case-studies`, `#how`, `#waitlist`. Those anchor links land at the top of the page instead of the section. Two header/footer generations are live: the homepage has the new nav, older pages (compare, builds, splash-jacks case study) still ship the old header with "Verticals" and `#sku`.
- **Loom placeholder is public on the homepage.** Section 01 shows "Demo recording in progress. Paste the Loom share URL into `LOOM_EMBED_URL` in `page.tsx`" — internal instructions visible to visitors.

## Customer sites
None yet — `~/Documents/businesses/day14/customers/` doesn't exist. Skipped per task spec.

## Recommended actions for Jack tomorrow
1. Grep `src/` for `#sku`, `#verticals`, `#faq` and swap to the new homepage anchors (or add anchor aliases on the homepage). ~15 min.
2. Drop the real Loom URL into `LOOM_EMBED_URL`, or hide the placeholder card until it exists. ~5 min.
3. Unify SiteHeader/SiteFooter — compare, builds, and the splash-jacks case study still render the pre-pivot header/footer. ~20 min.

— Confidence: 0.9. Every 200 + content claim verified by direct fetch tonight. Anchor breakage inferred from the fetched homepage section IDs, not clicked in a browser. Load time couldn't be measured precisely from the sandbox.
