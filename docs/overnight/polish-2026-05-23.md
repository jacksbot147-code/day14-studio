# Nightly polish — 2026-05-23

## day14-studio.vercel.app
- Status: ✓ healthy — every page served full HTML, no broken routes, no 404s in nav. All four issues below are copy/accuracy, not outages.
- Homepage load: not measurable in ms — the sandbox can't time a request and curl is network-blocked (HTTP 000, same as every prior night). WebFetch returned complete HTML on the first request for all 6 pages — no timeouts, no retries, "Day14" present in every response. Lighthouse can't run from here; the fallback check passes.
- Pages checked: 6 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (the required four), plus `/builds` and `/stack` (nav spot-check). Every one served full HTML. A deliberate 404 still can't be tested — the fetch tool only allows URLs it has already seen — so the not-found route itself stays unverified.
- Issues found: 4 — all copy/accuracy, every one a repeat. Nothing new broke tonight; nothing got fixed since 05-22 either.
- Customer sites: none. `~/Documents/businesses/day14/customers/` is empty — no `05-launch.md` files exist yet, so nothing to health-check there. (Sibling folders like `businesses/alignmd` and `businesses/hot-flash-co` exist but aren't customer launches under the day14 scope.)

## Issues to surface
- The Jobber price still splits $99 vs $169. The homepage hero and the compare-page hero both say "Jobber rents you a back-office for $99/mo forever." The compare table directly below lists "Jobber Connect — $169/mo" and the five-year math runs "$169/mo × 60 = $10,140." A prospect reads both numbers on the same screen — on the page whose entire pitch is the honest comparison. Flagged 05-21 and 05-22, unchanged.
- The hero rotator still renders "We shipmarketing sites" — no space between "ship" and the rotating noun. Sixth nightly polish in a row flagging this (05-17, 05-18, 05-20, 05-21, 05-22, tonight). Still a one-line fix.
- The builds count still triple-disagrees with itself. The homepage hero stat says "Live builds 3" and the Work section says "three live builds... all shipped, all live" — but that same section's Buildbridge card is labelled "Preview" and reads "Preview is currently SSO-gated." Then `/builds` says "Shipped — 01 build" and "In progress — 00 builds." Three answers, one site. Flagged 05-20, 05-21, 05-22, unchanged.
- The compare-page footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $99-vs-$169 split right above it is proof the fact-check never landed. Flagged 05-20 through 05-22, unchanged.

## Recommended actions for Jack tomorrow
1. Fix the Jobber $99/$169 split. It's on the "honest comparison" page and the homepage hero — exactly what a sharp prospect screenshots. Pick one number in `src/lib/site.ts`, make the compare hero, homepage line, table, and five-year math all agree, then date the footnote or cut it.
2. Reconcile the builds count. Either add Casamoré + Buildbridge to `/builds` as shipped entries, or drop the hero "3" and the "all live" line — and settle whether Buildbridge is "live" or "preview," because the homepage currently says both.
3. Fix the rotator space ("We ship_ marketing sites") in `src/lib/site.ts`. Six nights running — one line, and it stops making this report.
