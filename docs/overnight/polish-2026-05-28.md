# Nightly polish — 2026-05-28

## day14-studio.vercel.app
- Status: ✓ healthy — all four required pages return full HTML on first request, no 404s in nav, no routing surprises. Every issue below is copy/accuracy that's been on this list for nine nights running, not an outage.
- Homepage load: not measurable in ms — sandbox curl is network-blocked (HTTP 403 from the proxy on every URL); WebFetch has no timing readout. Fallback check passes: full HTML returned on first try, `Day14` present in the response body. Lighthouse can't run from here.
- Pages checked: 6 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (required four), plus `/builds`, `/stack`, `/case-studies/hot-flash-co`, and `/builds/splash-jacks-pools` as nav + footer spot-checks. All returned full HTML, all show the Day14 string. No customer sites to test — `~/Documents/businesses/day14/customers/` is still empty (no `05-launch.md` files exist yet).
- Issues found: 4 — every one is a repeat from the 05-25 nightly. Nothing broke today, nothing got fixed today.

## Issues to surface
- Jobber price is still a three-way split. Homepage hero and `/compare` hero both read "Jobber rents you a back-office for $99/mo forever." The compare table directly below says `$169/mo`, and the five-year math runs on `$169/mo × 60 mo = $10,140`. The `/compare` page meta description — the snippet Google shows in search results — still reads "Why not just use Jobber for $69/mo?" Three different numbers for one competitor, on the page whose entire pitch is the honest comparison. Ninth consecutive night.
- Hero rotator still renders "We shipmarketing sites" — no space between "ship" and the rotating noun. Ninth nightly polish in a row. One missing space in `src/lib/site.ts`.
- Builds count still contradicts itself. Homepage hero stat says `Live builds 3` and the Work section says "all shipped, all live" — but the Buildbridge card right there reads "Preview" and "Preview is currently SSO-gated", and `/builds` confirms `Shipped — 01 build, In progress — 00 builds`. Footer also links a fourth case study (Hot Flash Co) that the `3` doesn't count.
- `/compare` footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $69/$99/$169 split right above it is proof the fact-check never landed. Ninth night.

## Recommended actions for Jack tomorrow
1. Settle the Jobber number once. Pick one figure in `src/lib/site.ts`, then make the homepage hero, compare hero, compare table, five-year math, AND the `/compare` page metadata all agree. The $69 in the meta description is the worst of the three — it's the number Google shows in search.
2. Fix the rotator space ("We ship_ marketing sites") in `src/lib/site.ts`. Nine nights — one keystroke, and this report gets shorter tomorrow.
3. Reconcile the builds count: either drop the hero `3` and the "all live" line, or settle whether Buildbridge is live or preview — the homepage currently says both — and make `/builds` agree.
