# Day14 — Today's Autonomous Agenda

**Date:** 2026-05-15 (Friday)
**Window:** 10:30 ET → 18:00 ET (~7 hours while Jack is at work)
**Cadence:** 5 scheduled tasks, every 1–1.5h, with a final status report

## Why this shape

The wake-up report flagged Phase 1 (deploy) as the highest-leverage move
of the day. That work blocks on Jack's credentials (Vercel, domain
registrar, Cal.com, Stripe) and can't run autonomously. So today's
autonomous slate is the **next-highest-leverage work that doesn't gate
on Jack** — the Phase 2 template fork from splash-jacks-pools, plus
content polish that needs web-search to fact-check, plus the small
template fixes the overnight agents flagged.

By the time Jack is back at 17:30 ET, he should have:
- `studio-template-portal` actually forked + abstracted (not just a README placeholder)
- `studio-template-platform` scaffolded on top of Portal
- 5 blog drafts that are publish-ready (pricing fact-checked, claims softened where appropriate)
- The contact-form `message` bug fixed
- An end-of-day status report mirroring the wake-up format

## The 5 scheduled tasks

| Fire time (ET) | Task | Budget | Deliverable |
|---|---|---|---|
| 11:30 | **Portal template — Phase A: skeleton + audit** | 60m | studio-template-portal/ populated with stripped splash-jacks-pools tree; PLAN.md spells out the abstraction plan |
| 12:30 | **Portal template — Phase B: deep fork** | 120m | Pool→Item rename complete in schema + code; chemistry UI replaced by generic metadata; serviceType config in place; brand-swap script working |
| 14:30 | **Platform template scaffold** | 90m | studio-template-platform/ contains Portal + admin extensions + optional Storm Mode bundle (feature-flagged off) + Capacitor scaffold notes |
| 16:00 | **Content polish + small fixes** | 75m | Blog draft pricing fact-checked (Jobber/HCP/GHL/Squarespace), softeners applied, workers/subscribe.js `message` field plumbed, chatbot_system_prompt auto-propagation done |
| 17:15 | **End-of-day status report** | 45m | docs/overnight/06-end-of-day-status.md — mirrors the wake-up format |

## Why this order

Portal is the actual Day14 product. Every future customer build forks
from it. Doing this autonomously over 3 hours (Phase A + B back to back)
is exactly the kind of work that benefits from uninterrupted agent time
— no operator interruptions, no scope creep, just heads-down code
abstraction. Platform builds on Portal so it goes next. Content polish
needs web search (fact-checking Jobber's current price etc.) which is a
cheap parallel task. End-of-day report closes the loop the way the
wake-up report opened it.

## What's deliberately NOT in today's slate

- **Phase 1 (deploy)** — gates on Jack's auth + domain control
- **Phase 3 (sales / cold outreach)** — needs Jack as the human in the loop
- **Phase 2.4 follow-ups (auto-screenshot worker, approve-to-publish gate)** — sequenced after first paying customer
- **Phase 6 (vertical expansion templates)** — better done after the base templates are real (today's tasks)
- **New scheduled-task ops layer** — wait until the first customer build is live
