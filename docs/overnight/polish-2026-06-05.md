# Nightly polish — 2026-06-05

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: not measurable from sandbox (no external curl; WebFetch returned promptly, well under 3s)
- Pages checked: 7 total — all HTTP 200, all served real content
- Issues found: 2 (both content drift, not outages — listed below)

Checked: `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (the four required), plus nav spot-checks on `/stack`, `/builds`, `/work-with-us`. Every one resolved with full HTML and the expected "Day14" copy. No 404s, no broken nav links, no dead footer links surfaced. No customer sites to check yet — `~/Documents/businesses/day14/customers/` is empty.

## Issues to surface
- **Messaging split across the site.** The homepage and `/stack` have fully pivoted to the "Day14 OS — one operator, six businesses" story with subscription pricing ($79 / $299 / $999/mo). But `/about`, `/compare`, and `/case-studies` still run the old "14-day build studio" pitch with the $2,500 / $5,000 / $10,000 flat-SKU pricing. Both stories are live at once. A visitor landing on `/compare` sees a different company than one landing on `/`.
- **Nav bars don't match page to page.** `/compare` and the case study still render a nav with a "Verticals" link pointing at `/#verticals` and "Pricing" at `/#sku` — anchors the new OS homepage no longer has. Those links won't break, but they scroll to nothing.

## Recommended actions for Jack tomorrow
1. Decide the canonical story — OS-first or build-studio-first — then bring `/about`, `/compare`, and the case studies in line. The split is the biggest credibility leak right now.
2. Unify the header nav across every page; kill the stale `/#verticals` and `/#sku` anchors or re-add the sections they point to.
3. Nothing is down — this is a polish pass, not a fire. Ship the copy reconciliation when you next touch the repo, no rush tonight.
