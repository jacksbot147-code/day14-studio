# Nightly polish — 2026-05-22

## day14-studio.vercel.app
- Status: ✓ healthy — every page served, no broken routes. Every issue below is copy/accuracy, not an outage.
- Homepage load: not measurable in ms — sandbox curl is network-blocked (HTTP 000, same as every prior night). WebFetch returned full HTML on the first request for all 8 pages — no timeouts, no retries, "Day14" present in every response. Lighthouse can't run from here; the fallback check passes.
- Pages checked: 8 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (the required four), plus `/builds`, `/stack`, `/case-studies/hot-flash-co`, `/newsletter` (nav + footer spot-check). Every one served full HTML. No 404s in expected nav. Couldn't fire a deliberate 404 to test the not-found page — the fetch tool only allows URLs it's already seen — so the 404 route itself is unverified.
- Issues found: 4 — all copy/accuracy, every one a repeat. Nothing new broke tonight; nothing got fixed since 05-21 either.
- Customer sites: none. `~/Documents/businesses/day14/customers/` is empty — nothing to check there yet.

## Issues to surface
- The builds count triple-disagrees with itself. The homepage hero stat says "Live builds 3"; the Work section says "three live builds... all shipped, all live" — but that same section's Buildbridge card is labelled "Preview" and reads "Preview is currently SSO-gated." Then `/builds` itself says "Shipped — 01 build" (Splash Jacks only). Three different answers on one site. Flagged 05-20 and 05-21, unchanged.
- The Jobber price still contradicts itself. The compare hero — and the homepage hero copy — say "Jobber rents you a back-office for $99/mo forever." The compare table directly below lists "Jobber Connect — $169/mo" and the five-year math runs "$169/mo × 60 = $10,140." $99 and $169 on the same screen, on the page whose whole pitch is the honest comparison. Flagged 05-21, unchanged.
- The hero rotator still renders "We shipmarketing sites" — no space between "ship" and the rotating noun. Fifth nightly polish in a row flagging this (05-17, 05-18, 05-20, 05-21, tonight). Still a five-minute fix.
- The compare-page footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $99-vs-$169 split right above it is proof the fact-check never landed. Flagged 05-20 and 05-21, unchanged.

## Recommended actions for Jack tomorrow
1. Fix the Jobber $99/$169 split. It's on the "honest comparison" page and the homepage hero — exactly what a sharp prospect screenshots. Pick one number in `src/lib/site.ts`, make the compare hero, homepage line, table, and five-year math all agree, then date the footnote or cut it.
2. Reconcile the builds count. Either add Casamoré + Buildbridge to `/builds` as shipped entries, or drop the hero "3" and the "all live" line to match the log — and settle whether Buildbridge is "live" or "preview," because the homepage currently says both.
3. Fix the rotator space ("We ship_ marketing sites") in `src/lib/site.ts`. Five nights running — one line, and it stops making this report.
