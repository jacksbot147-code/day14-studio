# Nightly polish — 2026-06-10

**Target:** `src/lib/skills/uptime-monitor.ts` (hand-coded skill)
**Commit:** `299639e` (local only, on `redesign/apple-base44-2026-06-03`)
**Why this target:** the only one of the six original hand-coded skills with no test file, and the last ~10 polish nights were all site-health passes, not skill code.

## Gaps found vs SKILL.md spec

1. **Alert spam (spec hard rule 4 violated).** Alerts fired every cycle while a site stayed down (`recentDown >= 2` re-triggers each pass). Fixed: alert exactly once when the failure streak crosses the 10-min threshold, suppressed until recovery.
2. **No recovery follow-up (spec item 5).** Nothing logged when a site came back. Fixed: `uptime_recovered` audit entry with approx downtime, status, and TTFB.
3. **Discovery docstring lied.** Promised `02-status.md` frontmatter fallback; code only read `01-brand.json`. Fixed: `url:`/`domain:` frontmatter parsing added.
4. **No per-customer error isolation.** A single fs failure (e.g. unwritable log) aborted the whole cycle. Fixed: try/catch per customer; failures recorded as error checks.
5. **Alert details missing timing (spec hard rule 2).** Added `ms_to_ttfb` to alert payload; abort errors now read `timeout after 5000ms` instead of the generic abort message.
6. **`ALERT_AFTER_MIN` was dead code.** Now derives `ALERT_AFTER_CHECKS` from poll cadence.

Note: `logSkillInvocation` is handled centrally by `skill-runtime.ts:132` for every invocation — no in-skill call needed; verified rather than added.

## New test: `tests/uptime-monitor.test.ts`

8 cases following the `churn-risk-scorer.test.ts` TMP_HOME pattern: brand.json + status.md discovery, healthy-cycle logging, 3xx-up/5xx-down, the full alert lifecycle (no alert on 1st failure → alert on 2nd → suppressed on 3rd → recovery entry), blip-without-alert, network errors, multi-customer isolation, and `run()` outcome shape.

## Bonus fix (build was broken)

`tsc --noEmit` surfaced 3 `noUncheckedIndexedAccess` errors in `src/app/api/jarvis/route.ts` (unguarded regex match groups, shipped in last night's fleet-restoration commit). These broke `npm run build`. Three-line destructuring guard applied; repo-wide typecheck now exits 0.

## Verification

- `tsc --noEmit`: clean, repo-wide (was 3 errors before tonight).
- Vitest could not run in the sandbox — `node_modules` is darwin-arm64, rollup's linux native binary missing, and npm install is off-limits. Tests verified by typecheck + inspection. **Jack: run `npm test` on the Mac to confirm the 8 new cases pass.**

## Known limitation (documented in code)

`countRecentDown` only reads today's log file, so a failure streak resets at midnight UTC — worst case one re-alert per day during a multi-day outage. Acceptable; noted in the function docstring.
