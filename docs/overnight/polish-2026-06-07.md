# Nightly polish — 2026-06-07

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: not measurable from sandbox (no external curl; WebFetch returned promptly, well under 3s)
- Pages checked: 7 total — all HTTP 200, all served real content with the expected "Day14" copy
- Issues found: 3 (one is now live, not pending — the rest are content/nav drift)

Checked the four required pages — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` — plus nav spot-checks on `/stack`, `/work-with-us`, `/builds`. Every one resolved with full HTML and real content. No 404s, no dead nav, no broken links surfaced. No customer sites to check yet — `~/Documents/businesses/day14/customers/` doesn't exist.

## Issues to surface
- **The waitlist deadline has now expired on the homepage.** Section 05 still reads "I'm opening the waitlist today. The signal closes Sunday" with "by Sunday at 14:00 EDT." Today *is* Sunday, June 7 — 14:00 passed nine hours ago. Anyone landing on the most important page right now reads a dead countdown asking them to beat a time that's already gone. Flagged last night as "expires tomorrow." Tomorrow is now today, and it's behind us.
- **Three business stories still run side by side.** `/` is full Day14 OS ("one operator, six businesses," $79 / $299 / $999/mo Founder pricing). `/stack` sells the 20+ agent C-suite. But `/about`, `/compare`, and `/case-studies/splash-jacks-pools` still sell the "14-day build studio" with flat SKUs ($2,500 / $5,000 / $10,000). A visitor meets a different company depending on where they land.
- **Three different navs/footers, with stale anchors.** `/` and `/about` link Pricing → `/#pricing`; `/stack` and `/work-with-us` link Pricing → `/#sku`; `/compare`, `/builds`, and the Splash Jacks case study still render the old nav with "Verticals" → `/#verticals` and Pricing → `/#sku`. Those anchors don't exist on the OS homepage, so they scroll to nothing. Footers split the same way (new 4-column vs. old 3-column).

## Recommended actions for Jack tomorrow
1. Kill or rewrite the waitlist deadline first — it's the one thing on the site that's now factually wrong. Swap to a static "waitlist open" line or post where the signal actually landed.
2. Pick the canonical story (OS-first or build-studio-first) and bring `/about`, `/compare`, and the case studies in line. Biggest credibility leak, and it hasn't moved in days.
3. Unify header + footer across every page; kill the dead `/#verticals` and `/#sku` anchors. Nothing's down — this is polish, not a fire.
