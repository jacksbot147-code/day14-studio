# Nightly polish — 2026-05-17

**Scope:** day14-studio.vercel.app health check. `businesses/day14/customers/` is still empty — no launched customer sites to monitor yet.
**Confidence:** 0.85

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: not directly measurable from sandbox (WebFetch returns text, not timing). All four pages returned full HTML on first try with expected `<title>`, meta, and "Day14" present in body — no 4xx/5xx, no redirects to error pages.
- Pages checked: 4 / 4
  - `/` → 200, title "Day14 — Real business platforms, owned by you. Built in 14 days."
  - `/about` → 200, title "About · Day14"
  - `/compare` → 200, title "Day14 vs Jobber, Housecall Pro, GoHighLevel, Squarespace · Day14"
  - `/case-studies/splash-jacks-pools` → 200, title "Splash Jacks Pools — case study · Day14"
- Nav links spot-checked in rendered HTML: `/`, `/about`, `/compare`, `/#sku`, `/#how`, `/#case-studies`, `/builds`, `/verticals/{mobile-service,membership,food}`, `/case-studies/{splash-jacks-pools,casamore,buildbridge}`, `https://cal.com/day14/intro`, `mailto:hello@day14.us` — all present, none broken at the markup level.
- Lighthouse: skipped. Sandbox can't run it; WebFetch substitute used per the runbook fallback.

## Issues to surface
- **Hero stat row still reads "0 days / day 0 / 0 / $0" in static HTML.** Same observation as last night (polish-2026-05-16). Almost certainly the animated count-up component capturing its start frame. Worth Jack opening the site in a real browser once to confirm the numbers actually animate to {avg ship, day 14, live builds, $2,500}. If they don't, "Starting at $0" contradicts the SKU block on the same page and is a real bug.
- **OG/Twitter image origin is still `https://day14.us/...` while the site is served from `day14-studio.vercel.app`.** Unchanged from last night. If day14.us is live and canonical, this is correct. If not, social previews from the vercel.app URL will 404 the OG image. (Couldn't verify day14.us tonight — outside this fetch's provenance set.)
- **No `sitemap.xml`/`robots.txt` signal observed** in the static HTML of any page. Not a degradation — just hasn't shipped yet. Flag for the SEO pass.
- **No launched customer sites in `businesses/day14/customers/`** to monitor yet. When the first `05-launch.md` lands with `status: launched`, this task will pick it up.

## Recommended actions for Jack tomorrow
1. 60-second eyeball on the homepage in a real browser to confirm the hero stats animate to real values. If they're static zeros, that's tonight's only ship-blocker.
2. Resolve the OG image origin question — either point the vercel.app deploy at its own `metadataBase` or confirm day14.us is live and serving `/opengraph-image`. Paste-test in iMessage/Slack settles it.
3. Drop a `05-launch.md` under `businesses/day14/customers/{first-customer}/` once the first one ships so this task starts pinging real customer surfaces.
