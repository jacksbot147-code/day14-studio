# Nightly polish — 2026-05-18

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: WebFetch returned full HTML, no timing instrumentation available (sandbox curl blocked). All four pages returned content + Day14 in <single fetch>.
- Pages checked: 5 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools`, `/builds` (nav spot-check)
- Issues found: 3 — copy + placeholder data, no broken pages.

## Issues to surface
- Homepage hero stats are zeroed out: "Avg actual ship 0 days", "Live builds 0", "Starting at $0". Splash Jacks shipped — those should read real numbers (14 days, 1 live, $2,500). Looks lazy on a page that sells trust.
- Homepage rotator copy renders as "We ship**marketing sites**" with no space between verb and noun — either the marquee word is missing a leading space or the wrapper needs a padding-left. Fix in `src/lib/site.ts` or wherever the rotating noun list lives.
- Live-deploys ticker prints the same 5 commits twice in the DOM (marquee duplicate-for-loop trick). Looks fine animated, but two of the timestamps say "12m ago" both times — should stagger or de-dup the source data so it reads as one continuous feed.
- (Customer sites: none launched yet — `~/Documents/businesses/day14/customers/` is empty. Nothing to check.)

## Recommended actions for Jack tomorrow
1. Patch the hero stat block — wire Splash Jacks real numbers in `src/lib/site.ts` before the next intro call.
2. Fix the rotator spacing glitch (5-minute CSS or copy fix).
3. Once customer #1 is signed, add their `05-launch.md` so this nightly check starts covering live customer sites too.
