# Nightly polish — 2026-05-25

## day14-studio.vercel.app
- Status: ✓ healthy — every page returned full HTML on first request, no broken routes, no 404s in nav. Every issue below is copy/accuracy, not an outage. (Note: a polish-2026-05-25.md already existed from an earlier 09:18 ET run; this is the actual 23:00 ET nightly, overwriting with fresh findings — same conclusions, nothing changed on the site between the two runs.)
- Homepage load: not measurable in ms — sandbox can't time a request and curl is network-blocked (same as every prior night). WebFetch returned complete HTML first try for all four required pages plus two spot-checks, no timeouts, no retries, "Day14" present in every response. Lighthouse can't run from here; the fallback check passes.
- Pages checked: 6 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (the required four), plus `/builds` and `/case-studies/hot-flash-co` (nav + footer spot-check). All returned full HTML. A deliberate 404 still can't be tested — the fetch tool only allows URLs it has already seen.
- Issues found: 4 — all copy/accuracy, all repeats. Nothing new broke today; nothing got fixed today either.
- Customer sites: none. `~/Documents/businesses/day14/customers/` is empty — no `05-launch.md` files exist yet, so nothing to health-check there.

## Issues to surface
- The Jobber price is still a three-way split. The homepage hero and the compare-page hero both read "Jobber rents you a back-office for $99/mo forever." The compare table and the five-year math directly below run on $169/mo ("$169/mo × 60 = $10,140"). The `/compare` meta description — the snippet Google shows in search results — still reads "Why not just use Jobber for $69/mo?" Three numbers, one competitor, on the page whose entire pitch is the honest comparison. Flagged 05-21 through this morning; eighth consecutive night.
- The hero rotator still renders "We shipmarketing sites" — no space between "ship" and the rotating noun. Eighth nightly polish in a row flagging this (05-17, 05-18, 05-20, 05-21, 05-22, 05-23, 05-25 morning, tonight). Still one line in `src/lib/site.ts`.
- The builds count still disagrees with itself. The homepage hero stat says "Live builds 3" and the Work section says "three live builds... all shipped, all live" — but the Buildbridge card right there reads "Preview" and "Preview is currently SSO-gated," and `/builds` says "Shipped — 01 build, In progress — 00 builds." The footer also links a fourth case study, Hot Flash Co, that the "3" doesn't count (internal-tagged, defensible). Flagged 05-20 through this morning, unchanged.
- The compare-page footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $69/$99/$169 split right above it is proof the fact-check never landed. Flagged 05-20 through this morning, unchanged.

## Recommended actions for Jack tomorrow
1. Settle the Jobber number once. Pick one figure in `src/lib/site.ts`, then make the homepage hero, compare hero, compare table, five-year math, AND the `/compare` page metadata all agree. The $69 in the meta description is the worst of the three — it's the number that shows up in Google search results.
2. Reconcile the builds count. Either drop the hero "3" and the "all live" line, or settle whether Buildbridge is "live" or "preview" — the homepage currently says both — and make `/builds` agree.
3. Fix the rotator space ("We ship_ marketing sites") in `src/lib/site.ts`. Eight nights running — one line, and this report gets shorter.
