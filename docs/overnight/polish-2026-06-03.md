# Nightly polish — 2026-06-03

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: WebFetch returned full rendered text on first try (no retry, no timeout). Hard ms unavailable from the sandbox — Vercel Speed Insights is the source of truth here.
- Pages checked: 8 total (4 mandatory + 4 spot-check)
  - 200 on `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools`
  - 200 on `/stack`, `/builds`, `/work-with-us`, `/case-studies/hot-flash-co`, `/case-studies/casamore`
  - "Day14" present in every response body
- Issues found: 3 (all content drift, no broken links)

## Customer sites
- `~/Documents/businesses/day14/customers/` exists but is empty — no `05-launch.md` anywhere yet. Empire tenants (alignmd, hot-flash-co, life-loophole, kennum, day14-realty) live under `~/Documents/businesses/` directly, not under `day14/customers/`. Nothing to health-check until the launched-status convention is wired up.

## Issues to surface
- **Pitch drift between `/` and the rest of the site.** Homepage now leads with "One operator. Six businesses. One operating system." and Solo/Portfolio/Founder subscription tiers ($79 / $299 / $999). `/about`, `/compare`, `/work-with-us`, `/builds`, every case study still pitches the 14-day agency model with $2,500 / $5,000 / $10,000 fixed-price builds. A first-time visitor lands on OS, clicks About, reads agency. Not fatal — but the story isn't one story yet.
- **Nav anchor inconsistency.** Homepage nav links to `#pricing`; every other page's nav links to `#sku`. Both probably resolve, but one of them is going to silently break the next time the homepage gets re-sectioned.
- **`/compare` pricing footnote dated "May 2026."** It's June 3. Tiny credibility tax — the page itself says vendor pricing changes frequently.

## Recommended actions for Jack tomorrow
1. Decide whether `/about`, `/work-with-us`, and the case studies should adopt the OS framing or stay as the "day-job, still on offer" sub-story. Either is fine, but pick one and align copy.
2. Normalize the nav anchor: `#pricing` or `#sku`, not both. Grep the site and sed it in one pass.
3. Bump the `/compare` footnote to "as of June 2026" while you're in there.
