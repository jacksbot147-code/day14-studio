# Nightly polish — 2026-05-16

**Scope:** day14-studio.vercel.app health check. No launched customer sites yet (`businesses/day14/customers/` is empty).
**Confidence:** 0.85

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: not directly measurable from sandbox (WebFetch returns text, no timing). All four pages returned full HTML on first try with expected `<title>` and meta — no 4xx/5xx, no redirects to error pages.
- Pages checked: 4 / 4
  - `/` → 200, title "Day14 — Real business platforms, owned by you. Built in 14 days.", "Day14" present
  - `/about` → 200, title "About · Day14"
  - `/compare` → 200, title "Day14 vs Jobber, Housecall Pro, GoHighLevel, Squarespace · Day14"
  - `/case-studies/splash-jacks-pools` → 200, title "Splash Jacks Pools — case study · Day14"
- Nav links spot-checked in rendered HTML: `/`, `/about`, `/compare`, `/#sku`, `/#how`, `/#case-studies`, `/builds`, `/verticals/{mobile-service,membership,food}`, `/case-studies/{splash-jacks-pools,casamore,buildbridge}`, `https://cal.com/day14/intro`, `mailto:hello@day14.us` — all present, none broken at the markup level.
- Lighthouse: skipped. Sandbox can't run it; WebFetch substitute used per the runbook fallback.

## Issues to surface
- **Hero stat row reads "0 days / day 0 / 0 / $0".** The four-stat block under "We ship marketing sites" renders as zeros in static HTML (`Avg actual ship 0 days · Guaranteed by day 0 · Live builds 0 · Starting at $0`). Almost certainly an animated-counter component that counts up from 0 on the client — WebFetch can't run JS so it captures the start frame. Worth a 30-second eyeball check in a real browser to confirm "Starting at $0" lands on $2,500 and "Guaranteed by day 0" lands on day 14. If those are static zeros, that's a bug — "Starting at $0" contradicts the SKU pricing on the same page.
- **OG/Twitter image points to `day14.us`, page is served from `day14-studio.vercel.app`.** Every page's `og:image` / `twitter:image` is `https://day14.us/opengraph-image?...`. If `day14.us` is the real prod domain, this is correct and the vercel.app URL is just the preview. If `day14.us` isn't live yet, OG previews from the vercel.app URL will 404 the social card image. Verify in a Slack/iMessage paste preview.
- **No `lastmod`/sitemap signal observed.** Not necessarily a problem — just didn't see one in the static HTML. Flag for the SEO pass.
- **No launched customer sites to monitor yet.** `businesses/day14/customers/` is empty. Once Splash Jacks / Casamoré / Buildbridge ship under that folder with `05-launch.md` set to `launched`, this task picks them up.

## Recommended actions for Jack tomorrow
1. Open the homepage in a real browser and confirm the hero stat row animates to real numbers (Avg ship, day 14, live build count, $2,500). 60 seconds. If they're static zeros, that's the only ship-blocker from tonight.
2. Decide whether `day14.us` should be the canonical OG-image origin or whether the vercel.app deploy should serve its own OG images. One-line env or `metadataBase` change.
3. Once the first launched customer lands in `businesses/day14/customers/`, drop a `05-launch.md` with `status: launched` so this nightly task starts pinging it too.
