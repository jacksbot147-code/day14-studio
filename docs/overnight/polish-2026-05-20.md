# Nightly polish — 2026-05-20

## day14-studio.vercel.app
- Status: ✓ healthy — site is up, no broken pages.
- Homepage load: not measurable tonight (sandbox curl is network-blocked, HTTP 000). WebFetch returned full HTML on the first request for every page. Vercel API confirms the production deployment (`dca1fa6`, "sync: empire state 02:57") is state `READY`, built in ~50s, aliased to day14.us + www.day14.us. So: reachable over HTTPS, fresh deploy, healthy.
- Pages checked: 7 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (the required four), plus `/builds`, `/stack`, `/case-studies/hot-flash-co` (nav spot-check). Every one served full HTML with "Day14" present. No 404s.
- Issues found: 3 — all copy/accuracy, zero broken pages.
- Customer sites: none. `~/Documents/businesses/day14/customers/` is still empty — nothing to check there yet.

## Issues to surface
- The live-builds number doesn't agree with itself. The hero says "Live builds 3" and the Work section says "three live builds... all shipped, all live" — but `/builds` lists "Shipped — 01 build" (Splash Jacks only), and the homepage card tags Buildbridge "Preview" (SSO-gated). A prospect who clicks the "3" lands on a build log that proves 1. That's a trust hole on the page whose entire job is trust. The "Avg actual ship 9 days" stat sits in the same block and can't be verified against anything public either.
- The hero rotator still renders "We ship**marketing sites**" with no space — "ship" jammed straight into the rotating noun. Third nightly polish in a row flagging this (05-17, 05-18, tonight). It is a five-minute fix and it is still there.
- The compare-page footnote reads like an internal note left in customer-facing copy: "A formal pricing fact-check is scheduled for the same day this page was published." Vague, self-referential, and it quietly admits the table isn't verified.
- Good news, two prior flags cleared: hero stats are populated now (9 days / 3 / $2,500 — last week they were all zeros), and the live-deploys ticker timestamps stagger correctly instead of printing "12m ago" twice.

## Recommended actions for Jack tomorrow
1. Reconcile the builds story — either add Casamoré + Buildbridge to `/builds` as shipped entries, or drop the hero "3" to match what the log actually proves. Pick one; right now they argue.
2. Fix the rotator space ("We ship_ marketing sites") — copy fix in `src/lib/site.ts` or a `padding-left` on the marquee word. Flagged three nights running.
3. Finish the compare-page pricing fact-check and date it, or cut the footnote sentence. Don't ship a table that footnotes its own doubt.
