# Nightly polish — 2026-06-06

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: not measurable from sandbox (no external curl; WebFetch returned promptly, well under 3s)
- Pages checked: 7 total — all HTTP 200, all served real content with the expected "Day14" copy
- Issues found: 3 (all content/nav drift, not outages — listed below)

Checked the four required pages — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` — plus nav spot-checks on `/stack`, `/builds`, `/work-with-us`, `/case-studies/hot-flash-co`. Every one resolved with full HTML. No 404s, no dead nav, no broken links surfaced. No customer sites to check yet — `~/Documents/businesses/day14/customers/` is still empty.

## Issues to surface
- **The waitlist deadline on the homepage expires tomorrow.** The hero, section 05, and the meta description all say the signal "closes Sunday at 14:00 EDT" and "Waitlist open until Sunday." Sunday is June 7 — tomorrow. Past that, a live visitor reads a dead countdown on the most important page. This is the one time-boxed thing on the site.
- **Two business stories are still live at once — and the split has widened.** `/` and `/stack` now run the full "Day14 OS — one operator, six businesses" pitch with subscription pricing ($79 / $299 / $999/mo). But `/about`, `/compare`, `/work-with-us`, and both case studies still sell the "14-day build studio" with flat SKUs ($2,500 / $5,000 / $10,000). A visitor landing on `/compare` meets a different company than one landing on `/`.
- **Three different nav/footers across the site, with stale anchors.** `/`, `/about`, `/stack`, `/work-with-us`, `/hot-flash-co` use the new nav (Pricing → `/#pricing`); `/compare`, `/builds`, `/splash-jacks-pools` still render the old nav with "Verticals" → `/#verticals` and "Pricing" → `/#sku` — anchors the OS homepage no longer has, so they scroll to nothing. Footers are split the same way (4-column vs. old 3-column).

## Recommended actions for Jack tomorrow
1. Settle the waitlist deadline first — extend it, swap to a static "waitlist open" line, or post where the signal landed. It goes stale at 14:00 tomorrow.
2. Pick the canonical story (OS-first or build-studio-first) and bring `/about`, `/compare`, `/work-with-us`, and the case studies in line. This is the biggest credibility leak, and it's grown.
3. Unify header + footer across every page; kill the stale `/#verticals` and `/#sku` anchors. Nothing's down — this is polish, not a fire. Ship it next time you touch the repo.
