# Nightly polish — 2026-05-21

## day14-studio.vercel.app
- Status: ✓ healthy — every page up, no broken routes. All issues below are copy/accuracy, not outages.
- Homepage load: not measurable tonight (sandbox curl is network-blocked, HTTP 000). WebFetch returned full HTML on the first request for all 9 pages — no timeouts, "Day14" present in every response, fast enough to serve. Lighthouse can't run from here; fallback check passes.
- Pages checked: 9 total — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` (the required four), plus `/builds`, `/stack`, `/case-studies/casamore`, `/case-studies/buildbridge`, `/case-studies/hot-flash-co` (nav + footer spot-check). Every one served full HTML. No 404s.
- Issues found: 4 — all copy/accuracy, zero broken pages.
- Customer sites: none. `~/Documents/businesses/day14/customers/` is empty — nothing to check there yet.

## Issues to surface
- NEW — the compare page contradicts itself on Jobber's price. The hero says "Jobber rents you a back-office for $99/mo forever," then the table directly below lists "Jobber Connect — $169/mo" and the five-year math runs "$169/mo × 60 = $10,140." A prospect reads $99 and $169 on the same screen — on the page whose whole pitch is "the honest comparison." The homepage repeats the $99 line too. The internal math is consistent at $169; only the prose is stale.
- The hero rotator still renders "We ship**marketing sites**" with no space — "ship" jammed into the rotating noun. Fourth nightly polish in a row flagging this (05-17, 05-18, 05-20, tonight). Five-minute fix, still shipped.
- The builds story still argues with itself. Hero says "Live builds 3" and the Work section says "three live builds... all shipped, all live" — but `/builds` still reads "Shipped — 01 build" (Splash Jacks only). Casamoré and Buildbridge have full case studies that never made it onto the build log. Flagged 05-20, unchanged.
- The compare-page footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $99-vs-$169 split above is proof the fact-check never happened. Flagged 05-20, unchanged.

## Recommended actions for Jack tomorrow
1. Fix the Jobber price first. It's on the "honest comparison" page and it's the kind of thing a sharp prospect screenshots. Pick $99 or $169, set it once in `src/lib/site.ts`, and make the compare hero, homepage line, table, and five-year math all agree. Then date the footnote or cut it.
2. Fix the rotator space ("We ship_ marketing sites") in `src/lib/site.ts` or with padding on the marquee word. Four nights running.
3. Reconcile the builds count — add Casamoré + Buildbridge to `/builds` as shipped entries, or drop the hero "3" to match the log. They've disagreed two nights now.
