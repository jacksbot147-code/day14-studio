# Nightly polish — 2026-05-30

## day14-studio.vercel.app
- Status: ✓ healthy — all four required pages return full HTML on first request, no 404s in nav, the chatbot mounts, the live-deploys ticker renders. Every issue below is the same copy/accuracy bug we've been logging for ten nights running, not an outage.
- Homepage load: not measurable in ms — sandbox curl is network-blocked (proxy returns 000 on every URL); WebFetch ships HTML but no timing readout. Fallback check passes: full markup returned first try, `Day14` string present, title tag is the canonical "Real business platforms, owned by you. Built in 14 days."
- Pages checked: 4 required (`/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools`) — all 200, all full HTML. No customer sites in scope: `~/Documents/businesses/day14/customers/` is still empty (zero `05-launch.md` files), so the per-customer loop is a no-op for the eleventh consecutive night.
- Issues found: 4 — every one is a repeat from polish-2026-05-28. Nothing broke today, nothing got fixed today.

## Issues to surface
- Jobber price is still a three-way split. Homepage hero and `/compare` hero both read "Jobber rents you a back-office for $99/mo forever." The compare table directly below says `$169/mo`, and the five-year math runs on `$169/mo × 60 mo = $10,140`. The `/compare` meta description — the snippet Google shows in search results — still reads "Why not just use Jobber for $69/mo?" Three different numbers for one competitor, on the page whose entire pitch is the honest comparison. Tenth consecutive night.
- Hero rotator still renders "We shipmarketing sites" — no space between "ship" and the rotating noun. Tenth nightly polish in a row. One missing space in `src/lib/site.ts`.
- Builds count still contradicts itself. Homepage hero stat says `Live builds 3` and the Work section says "all shipped, all live" — but the Buildbridge card right there reads "Preview" and "Preview is currently SSO-gated", and the footer links a fourth case study (Hot Flash Co) that the `3` doesn't count.
- `/compare` footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $69/$99/$169 split right above it is proof the fact-check never landed. Tenth night.

## Recommended actions for Jack tomorrow
1. Settle the Jobber number once. Pick one figure in `src/lib/site.ts`, then make the homepage hero, compare hero, compare table, five-year math, AND the `/compare` page metadata all agree. The $69 in the meta description is still the worst of the three — it's the number Google shows in search.
2. Fix the rotator space ("We ship_ marketing sites") in `src/lib/site.ts`. Ten nights — one keystroke, and tomorrow's report gets shorter.
3. Reconcile the builds count: either drop the hero `3` and the "all live" line, or settle whether Buildbridge is live or preview — the homepage currently says both — and make the footer's four case-study links match whatever number you keep.
