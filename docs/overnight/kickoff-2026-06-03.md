# Daily kickoff — 2026-06-03 (Wednesday)

**Run:** day14-os-daily-kickoff (autonomous, 09:00 ET)
**Confidence:** 0.65

## Yesterday's wins (Tuesday Jun 2)

- **First real source commit since May 30.** `8d195e9` at 16:38 ET — 44 files, +2,612 / -692. The miss-streak broke. *Source: `eod-2026-06-02.md`.*
- **Pricing nav button works again.** Six files migrated `/#sku` → `/#pricing` to match the new OS-waitlist homepage anchor. The 404 from Monday is gone. Priority #1 from yesterday's kickoff is half-done — the anchor is fixed; the cross-page voice match is not. *Source: `eod-2026-06-02.md`.*
- **/admin/bulk-signoff ships ~36 queued picks behind one screen.** Approve/Skip server actions wired, reads all four tenant inboxes, AdminNav entry added, banner on `/admin/inbox` at ≥5 pending. *Source: `eod-2026-06-02.md` §N9/N10.*

Yesterday's priorities #2 (Council 0001 Outcome / API key paste) and #3 (Loom placeholder + copy bugs + telegram-poller) — both carry over untouched.

## Today's 3 priorities (ranked)

Week 3 of the 25-day plan (May 30 – Jun 5) — day 5 of 7. Week's targets: pricing experiments and skill harvest #2. Both still untouched. The plan is now 1-for-16 since May 16 (yesterday's commit half-counts kickoff #1). Re-baseline is overdue.

1. **The ten-minute unblock: restart telegram-poller and paste the `EnvironmentVariables` block into the daemon plists.** Yesterday's EOD called this the "tomorrow's first action" — two blockers cleared in one session. Telegram poller heartbeat last touched May 23 05:29 ET (~11 days down); `launchctl unload` then `load` is one command. The 13 FATAL lines in the 17:00 ET DevOps report (brand-steward, cfo-agent, six hot-flash engines) all trace to launchctl daemons not sourcing `studio/.env.local` — add `EnvironmentVariables` to each poller plist, or one shared `~/Library/LaunchAgents/com.day14.env.plist` loaded first. *Source: `eod-2026-06-02.md` Blockers #1 and #2, "Tomorrow's first action."*

2. **Finish what yesterday's commit started: consolidate SiteHeader/SiteFooter and reconcile the OS pivot on `/about` + `/compare`.** Last night's polish found two different headers and two different footers shipped to prod, and the Day14 OS pitch only lands on `/`. Same domain, two pitches — same break Monday's kickoff flagged. Yesterday's commit fixed the Pricing anchor; today's job is the cross-page reframe (or move `/about` + `/compare` under `/legacy/`). ~30 minutes if the call is "OS pivot is real." *Source: `polish-2026-06-02.md` recs #1–2; `kickoff-2026-06-02.md` priority #1; `day14-os-laptop-interim-plan.md` Week 3.*

3. **Council 0001 Outcome block OR Anthropic API key — pick one and ship it before EOD.** Day 15 open. Three weekly auto-reviews logged `Aged: poorly-aged`; three consecutive kickoffs + EODs have asked; zero delivered. Strip the kickoff to this one until it lands, or formally downgrade Council to advisory per `council-review-2026-06-01.md` Pattern #2. *Source: `eod-2026-06-02.md` "What was planned but didn't ship" #1; `_shared/operator-todos.json` todo-1.*

## Decisions waiting on Jack

- **Council 0001: enforcement or advisory.** Day 15 open. T-72h/T-24h/T-0 nudges or formally downgrade. Until this is made, every Council inherits 0001's failure mode.
- **Re-baseline the 25-day plan or stop citing it.** 1-for-16. Week 3 is 1-for-4. Three EODs and two kickoffs have called for the re-baseline; no action.
- **OS pivot scope.** Yesterday's commit committed to the OS pitch on the homepage and routed the Pricing nav at it. `/about` and `/compare` still sell the 14-day SKU. Pick one for the whole site or route the OS pitch behind `/os`.

## Calendar awareness

- **No 30-day-aged Council decision today.** 0001 logged 2026-05-15; re-read lands **Jun 14** — inside the Mac mini cutover window (Jun 10–17). Worth surfacing now: that re-read will collide with cutover prep.
- **Mac mini cutover opens in 7 days** (Jun 10). Week 4 (Jun 6–9) is laptop polish + rsync plan. Four working days left on Week 3.
- **No external calendar events surfaced.** This kickoff doesn't see the Calendar MCP.

## Coffee-and-go: the single first action

Open Terminal. Run `launchctl unload ~/Library/LaunchAgents/com.day14.telegram-poller.plist && launchctl load ~/Library/LaunchAgents/com.day14.telegram-poller.plist`. Verify the heartbeat log writes a fresh line. That's two minutes — telegram poller back, one blocker cleared. While the terminal is open, paste the `EnvironmentVariables` block into the poller plists and reload. The 17:00 ET ops report opens green tonight for the first time in 12 days.

---

*Drafts only. Cross-reference: `eod-2026-06-02.md`, `polish-2026-06-02.md`, `kickoff-2026-06-02.md`, `day14-os-laptop-interim-plan.md` Week 3, `council-log/0001-first-customer-acquisition.md`, `council-review-2026-06-01.md`, `_shared/operator-todos.json` todo-1 / todo-28.*
