# Nightly polish — 2026-06-08

## day14-studio.vercel.app
- Status: ✓ healthy
- Homepage load: not measurable from sandbox (no external curl; WebFetch returned promptly, well under 3s)
- Pages checked: 5 total — all served real HTML with the expected "Day14" copy, no 404s
- Issues found: 3 — all content/nav drift, all carried over from last night, none moved

Checked the four required pages — `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` — plus `/builds`. Every one resolved with full content and real copy. Nothing down, no dead 404s, no broken links surfaced. No customer sites to check yet: `~/Documents/businesses/day14/customers/` exists but holds no `05-launch.md` manifests, so nothing reads as `launched`. Worth noting the OS homepage now advertises six live tenant domains (alignmd.com, lifeloophole.com, day14-realty.us, hotflashco.com, kennum.us, day14.us) that aren't yet wired into the launch-manifest path this check keys off — they're flying blind tonight.

## Issues to surface
- **The waitlist deadline is now firmly in the past — second night running.** Section 05 still reads "I'm opening the waitlist today. The signal closes Sunday" with "by Sunday at 14:00 EDT." It's Monday, June 8. That deadline passed ~36 hours ago. The meta description ("Waitlist open until Sunday") carries the same dead clock into every search and social preview. This is the one thing on the most important page that's now factually wrong, and it's been wrong since yesterday's report flagged it.
- **Three pages still render the OLD nav + footer with dead anchors.** `/compare`, `/builds`, and the Splash Jacks case study all serve the pre-OS header — "Verticals" → `/#verticals`, "Pricing" → `/#sku` — plus the old 3-column footer linking `/#sku`. Those anchors don't exist on the OS homepage, so they scroll to nothing. Meanwhile `/` and `/about` use the new nav (Pricing → `/#pricing`, Stack, Hire us) and 4-column footer. Unchanged from last night.
- **Two companies depending on where you land.** `/` is full Day14 OS — "one operator, six businesses," $79 / $299 / $999/mo Founder tiers. But `/about`, `/compare`, and the case study still sell the "14-day build studio" with flat SKUs ($2,500 / $5,000 / $10,000). A visitor meets a different business depending on the URL. Same credibility leak as yesterday, still open.

## Recommended actions for Jack tomorrow
1. Kill the waitlist countdown first — it's the only outright-false thing on the site, and it's now leaking into meta/social previews too. Swap to a static "waitlist open" line or post where the signal actually landed.
2. Unify header + footer across `/compare`, `/builds`, and the case studies; kill the dead `/#verticals` and `/#sku` anchors. Pure mechanical fix, untouched for days.
3. Pick the canonical story (OS-first or build-studio-first) and bring `/about`, `/compare`, and the case study in line. Nothing's down — this is polish, not a fire.
