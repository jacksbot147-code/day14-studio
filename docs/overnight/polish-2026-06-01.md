# Nightly polish — 2026-06-01

## day14-studio.vercel.app
- Status: ! degraded — every page returns full HTML on first request and no link in the nav 404s, but the site is now visibly two products glued to one domain. The homepage has pivoted to "Day14 OS — multi-tenant operator OS" with a Solo $79 / Portfolio $299 / Founder $999 waitlist; `/about`, `/compare`, and the case study still sell the 14-day fixed-price platform build at $5k–$10k. Voice break, not an outage.
- Homepage load: not measurable in ms — WebFetch returns no timing readout and the sandbox curl is still proxy-blocked (000). Fallback passes: full markup on first try, `Day14` string present, canonical title is now "Day14 OS — The operating system for solopreneurs running multiple businesses."
- Pages checked: 4 required (`/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools`) — all 200, all full HTML. No customer sites in scope: `~/Documents/businesses/day14/customers/` is still empty (zero `05-launch.md` files), so the per-customer loop is a no-op for the twelfth consecutive night.
- Issues found: 5 — three are repeats from polish-2026-05-30, two are new and arrived with today's homepage pivot.

## Issues to surface
- **NEW — Product split between homepage and the rest of the site.** Homepage is now "Day14 OS," a SaaS waitlist for an operator OS at $79/$299/$999/mo. `/about` is still "Day14 is Jack shipping fixed-price 14-day builds for customers." `/compare` is still the Jobber/Housecall/GoHighLevel honest-table for the Portal SKU at $5k + $199/mo. The case study is the Splash Jacks Platform build. A visitor who clicks "Hire us" or "About" from the new hero lands in a different product. Same domain, two pitches.
- **NEW — Jump links from the new homepage's own nav point at anchors that no longer exist.** Nav and footer both link `Pricing → /#sku`, and the footer lists `Site / Portal / Platform → /#sku`. The new homepage's pricing section is the OS tiers (Solo/Portfolio/Founder); there is no `#sku` block on `/` anymore. The footer Site/Portal/Platform links jump to the OS pricing instead.
- **NEW — Loom placeholder is shipped live and public.** The new homepage's demo section reads: "loom · day14-os demo — Demo recording in progress. Paste the Loom share URL into `LOOM_EMBED_URL` in `page.tsx`." That is a build TODO sitting on the page whose entire CTA is "watch the 4-minute demo." Waitlist visitors see it.
- Jobber price is still a three-way split on `/compare`: hero says "Jobber rents you a back-office for $99/mo forever," table says `$169/mo`, five-year math runs `$169 × 60 = $10,140`, and the meta description Google shows still reads "Why not just use Jobber for $69/mo?" Twelfth consecutive night. (Homepage no longer mentions Jobber — that pitch lives only on `/compare` now.)
- `/compare` footnote still reads "A formal pricing fact-check is scheduled for the same day this page was published." Self-referential — and the $69/$99/$169 split right above it is proof the fact-check never landed. Twelfth night.

## Recommended actions for Jack tomorrow
1. Decide what `day14-studio.vercel.app` is selling this week. If the OS pivot is the front door, then `/about` and `/compare` need to be reframed (or routed to a `/legacy/` namespace), the nav needs an OS-aware information architecture, and the case study has to read as proof-of-OS, not proof-of-SKU. If the OS is still a side bet, move the new hero behind `/os` and put the old SKU homepage back. Either is fine — but shipping both as the same domain is the voice break.
2. Either delete the `LOOM_EMBED_URL` placeholder block or paste a real Loom share URL into `page.tsx`. The waitlist closes Sunday; a public-facing "demo recording in progress" note on the page whose CTA is "watch the demo" is the single sharpest credibility leak tonight.
3. Fix the homepage anchors and pick one Jobber number. `Pricing → /#sku` in nav and footer needs to land on the OS pricing section (rename the anchor to `#sku` or repoint the links to `#pricing`), and the `/compare` hero/table/math/meta-description all need to agree on one Jobber figure — the $69 in the search snippet is still the worst of the three. One pass through `src/lib/site.ts` resolves both.

## Re-verify pass (duplicate fire detected, 23:xx ET)
- Scheduler fired the nightly-polish task a second time tonight; MASTER_LOG already has the 23:00 ET completion line, so this stanza is a verification appendix, not a new log entry. No second `MASTER_LOG` line written.
- Re-fetched `/`, `/about`, `/compare`, `/case-studies/splash-jacks-pools` — all four still return 200 with full HTML on first request. OS pitch on `/`, SKU pitch on the other three. No change since the main report was written.
- Loom placeholder still public on `/`. Jobber price still split $99/$169/$10,140/$69 across `/compare`. Twelfth night unchanged.
- New surface area to add to the product-split issue: `/stack` and `/work-with-us` are both linked from the new homepage nav. `/stack` doubles down on the OS pitch ("20+ AI agents… the C-suite, hired… the autonomous loops"). `/work-with-us` doubles down on the SKU pitch ("fixed scope, owned by you on day fourteen"). Same nav, same domain, opposite products on adjacent links — sharpens issue #1 above.
